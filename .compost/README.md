# `.compost/` — local recovery pile

**Gitignored** except this file (see root `.gitignore`). Use for **SHAKE** / TIDY steps when replacing trees or discarding experiments.

| Action | Meaning |
| --- | --- |
| **Move here** | Copy or move superseded folders/files under `.compost/<label-or-date>/` |
| **Restore** | Copy back into the canonical path before you rely on git alone |
| **Destroy** | Permanent delete only after explicit confirmation (family DESTROY/REBIRTH specs) |

Do not commit large binary dumps; keep this directory local unless promoting to a tracked archive on purpose.

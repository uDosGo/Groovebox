# Songscribe frontend (containers/songscribe/repo) — copy to .env.local
# Backend: https://github.com/gabe-serna/songscribe-api (Python, stem split + MIDI)
# Run the API locally, then set its public URL here (no trailing slash required).

NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

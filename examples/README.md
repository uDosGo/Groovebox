# Examples

These example files are the public teaching surface for `uDOS-groovebox`.

- `basic-pattern.json` shows a standalone pattern document
- `basic-songscribe-pattern.json` shows a Songscribe bridge payload rendered into
  Groovebox-friendly structure
- `compiled-demo-pattern.json` shows the compiled markdown-to-pattern output

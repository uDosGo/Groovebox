{
  "name": "groovebox",
  "private": true,
  "version": "0.1.0-dev",
  "description": "Groovebox \u2014 Vue 3 + PicoCSS + USX tokens + uCore snackbar/MCP alignment",
  "type": "module",
  "workspaces": [
    "groovebox-vue",
    "packages/*"
  ],
  "scripts": {
    "dev": "cd groovebox-vue && vite",
    "build": "cd groovebox-vue && vue-tsc && vite build",
    "preview": "cd groovebox-vue && vite preview"
  },
  "engines": {
    "node": ">=18"
  }
}

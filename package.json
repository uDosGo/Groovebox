{
  "name": "groovebox888",
  "private": true,
  "version": "1.0.0",
  "description": "GrooveBox888 — groovebox monorepo root (FastAPI app + optional packages/* for Songscribe-aligned design tokens).",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "docs": "node -e \"console.log('No JS build yet. See docs/groovebox-songscribe-convergence.md')\""
  },
  "engines": {
    "node": ">=18"
  }
}
